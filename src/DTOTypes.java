
/**
 * This enum represents the different types of data that can be used in DTOs (Data Transfer Objects).
 * The available types are STRING, INTEGER, DATE, and FLOAT.
 */
public enum DTOTypes {
    STRING,
    INTEGER,
    DATE,
    FLOAT
}